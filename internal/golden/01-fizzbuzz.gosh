#!/usr/bin/env gosh

var i = 1
for i = 1; i <= 100; i++ {
	var m3 = (i%3 == 0)
	var m5 = (i%5 == 0)

	switch {
	case m3 && m5:
		println("FizzBuzz")
	case m3:
		println("Fizz")
	case m5:
		println("Buzz")
	default:
		println(i)
	}
}
